#pragma once

// TODO: Move GameState content to here, use world.play(...) on GameState::update() to run code here

class ClientWorld : public sf::Drawable, public sf::Transformable {
public:
    ClientWorld() {
       
    }
    

    void update(sf::Time deltaTime) {
        
    }
private:
    void draw(sf::RenderTarget& target, sf::RenderStates states) const override {
        
    }
private:
    
};